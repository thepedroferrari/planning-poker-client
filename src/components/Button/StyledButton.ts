import { motion } from "framer-motion"
import styled from "styled-components"

export const StyledButton = styled(motion.button)`
  color: white;
  background: rgba(40, 90, 250);
  border-radius: var(--border-radius);
  box-shadow: rgba(0, 0, 5px, 0.15);
`
